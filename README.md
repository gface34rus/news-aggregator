# 📰 News Aggregator

Пет-проект агрегатора новостей на Java (Spring Boot).
Приложение собирает новости из RSS лент, сохраняет их в базу данных, показывает через веб-интерфейс, предоставляет REST API и отправляет обновления в Telegram.

## 🚀 Функционал

*   **RSS Парсинг**: Автоматический сбор новостей (например, с Habr) каждые 10 минут.
*   **Web Интерфейс**: Просмотр ленты новостей и поиск по ключевым словам.
*   **REST API**: JSON API для получения списка статей.
*   **Telegram Bot**: Уведомления о новых статьях в реальном времени.
*   **Персистентность**: Хранение данных в локальной базе H2 (файловый режим).
*   **Фильтрация**: Поиск новостей по заголовку и содержимому.

## 🛠 Технологии

*   **Java 21**: Язык программирования.
*   **Spring Boot 3.2.1**: Основной фреймворк (Web, Data JPA, Scheduling).
*   **H2 Database**: Встраиваемая БД (хранит данные в `./data`).
*   **ROME**: Библиотека для парсинга RSS.
*   **Telegram Bots API**: Интеграция с Telegram.
*   **Thymeleaf & Bootstrap 5**: Веб-интерфейс.
*   **Lombok**: Сокращение бойлерплейт-кода.

## ⚙️ Настройка и Запуск

### Предварительные требования
*   JDK 17+ (рекомендуется 21).
*   Maven.

### Настройка
Откройте `src/main/resources/application.properties` и укажите токен вашего бота:
```properties
bot.name=ВашБот
bot.token=ВАШ_ТОКЕН_ОТ_BOTFATHER
```

### Запуск
```bash
mvn spring-boot:run
```
Или запустите через вашу IDE (класс `NewsAggregatorApplication`).

## 🖥 Использование

### Веб-интерфейс
Откройте в браузере: [http://localhost:8080/](http://localhost:8080/)

### API
*   `GET /api/articles?page=0&size=10` — Получить список новостей (JSON).

### Telegram Bot
Найдите своего бота в Telegram и нажмите **/start**.
Вы будете получать уведомления о каждой новой новости, найденной парсером.
